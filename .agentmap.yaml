meta:
  project: agent-resources
  version: 1
  updated: "2026-03-01"
  stack:
    - claude-code
    - bash
    - markdown

tree:
  marketplace.json: "plugin registry, lists available plugins"
  CLAUDE.md: "project conventions and rules"
  plugins/:
    agentmap/:
      plugin.json: "plugin metadata: skills list"
      install.sh: "copies skills + docs to ~/.claude/"
      example.agentmap.yaml: "reference agentmap for other projects"
      skills/:
        agentmap-generate/: "skill: generate .agentmap.yaml"
        agentmap-init/: "skill: check for and guide .agentmap.yaml setup"
        agentmap-read/: "skill: consume .agentmap.yaml"
    jinglemansweep/:
      plugin.json: "plugin metadata: skills + agents lists (v0.4.0)"
      install.sh: "copies skills + agents to ~/.claude/, cleans old artefacts"
      skills/:
        jms-git/: "skill: structured GitHub operations via gh CLI"
    jplan/:
      plugin.json: "plugin metadata: skills + agents lists"
      install.sh: "copies skills + agents to ~/.claude/"
      agents/:
        jp-worker-dev.md: "agent: general-purpose developer with domain skill routing"
      skills/:
        jp-setup/: "skill: init .plans directory"
        jp-plan/: "skill: create new plan phase"
        jp-prd/: "skill: generate PRD from prompt"
        jp-prd-review/: "skill: review PRD against prompt"
        jp-task-list/: "skill: convert PRD to task list"
        jp-task-review/: "skill: validate task list against PRD"
        jp-execute/: "skill: execute task list with review-fix loop"
        jp-task-validate/: "skill: run post-task validation checks"
        jp-codereview/: "skill: holistic code review"
        jp-codereview-fix/: "skill: apply targeted fixes for review issues"
        jp-summary/: "skill: generate final summary report"
        jp-quick/: "skill: end-to-end pipeline orchestration"
        jp-persona-python/: "domain skill: Python conventions and quality gates"
        jp-persona-nodejs/: "domain skill: Node.js/TypeScript conventions"
        jp-persona-frontend/: "domain skill: frontend/UI conventions"
        jp-persona-devops/: "domain skill: infrastructure and CI/CD conventions"
        jp-persona-docs/: "domain skill: documentation conventions"
        jp-persona-agent-skills/: "domain skill: skill authoring conventions"
  .plans/: "planning phase artifacts (generated)"
  .github/workflows/: "CI: pre-commit hooks"

conventions:
  - plugins under plugins/<name>/: plugin.json, install.sh, skills/, agents/
  - one SKILL.md per skill directory: self-contained definition
  - agents are single markdown files: in agents/ directory
  - jms- prefix for jinglemansweep plugin: agentmap- prefix for agentmap plugin
  - jp- prefix for jplan plugin: jp-* for planning pipeline skills, jp-persona-* for domain skills
  - install.sh copies to ~/.claude/: CLAUDE_DIR overridable

# Task list generated from PRD: .plans/2026/03/01/07-tidying/prd.md
# Generated by: /jp-plan-task-breakdown

tasks:
  - id: "TASK-001"
    title: "Delete jms-git-push skill directory"
    description: "Delete the entire `plugins/jinglemansweep/skills/jms-git-push/` directory and all its contents. This directory currently contains a single `SKILL.md` file. The skill is no longer used and should be removed from the filesystem."
    requirements:
      - "REQ-001"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "The directory plugins/jinglemansweep/skills/jms-git-push/ no longer exists"
      - "No files from the deleted skill remain anywhere in the repository"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jinglemansweep/skills/jms-git-push/SKILL.md"

  - id: "TASK-002"
    title: "Remove jms-git-push from jinglemansweep plugin.json"
    description: "Edit `plugins/jinglemansweep/plugin.json` and remove the `\"jms-git-push\"` entry from the `skills` array. The current skills array is: [\"jms-git\", \"jms-git-push\", \"jms-role-python\", \"jms-role-nodejs\", \"jms-role-frontend\", \"jms-role-devops\", \"jms-role-docs\", \"jms-role-agent-skills\"]. After the edit, the array should be: [\"jms-git\", \"jms-role-python\", \"jms-role-nodejs\", \"jms-role-frontend\", \"jms-role-devops\", \"jms-role-docs\", \"jms-role-agent-skills\"]. Ensure the JSON remains valid."
    requirements:
      - "REQ-002"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "plugins/jinglemansweep/plugin.json does not contain jms-git-push in its skills array"
      - "The JSON file is valid (parseable without errors)"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jinglemansweep/plugin.json"

  - id: "TASK-003"
    title: "Delete jp-planner agent file"
    description: "Delete the file `plugins/jplan/agents/jp-planner.md`. This agent definition is no longer used in practice. The `jp-developer.md` agent in the same directory should remain untouched."
    requirements:
      - "REQ-003"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "The file plugins/jplan/agents/jp-planner.md no longer exists"
      - "The file plugins/jplan/agents/jp-developer.md still exists and is unmodified"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/agents/jp-planner.md"

  - id: "TASK-004"
    title: "Remove jp-planner from jplan plugin.json"
    description: "Edit `plugins/jplan/plugin.json` and remove the `\"jp-planner\"` entry from the `agents` array. The current agents array is: [\"jp-planner\", \"jp-developer\"]. After the edit, the array should be: [\"jp-developer\"]. Ensure the JSON remains valid."
    requirements:
      - "REQ-004"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "plugins/jplan/plugin.json does not contain jp-planner in its agents array"
      - "The JSON file is valid (parseable without errors)"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/plugin.json"

  - id: "TASK-005"
    title: "Update README.md to remove references"
    description: |
      Edit `README.md` to remove all references to `jms-git-push` and `jp-planner`. Three sections need changes:

      1. **Directory tree** (around line 27): Remove the line `      jms-git-push/`.
      2. **jinglemansweep skills list** (around line 79): Remove the line `- \`jms-git-push\` — Automate branch creation, PR submission, and auto-merge for accidental default-branch commits`.
      3. **jplan agents list** (around lines 117-119): Remove the `jp-planner` entry and its description. Keep the `jp-developer` entry.

      Ensure surrounding formatting (blank lines, indentation, list continuity) remains clean after the removals.
    requirements:
      - "REQ-005"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
      - "TASK-004"
    suggested_role: "docs"
    acceptance_criteria:
      - "The directory tree in README.md does not list jms-git-push/"
      - "The jinglemansweep skills section does not mention jms-git-push"
      - "The jplan agents section does not mention jp-planner"
      - "The README is well-formed Markdown with no broken formatting"
    estimated_complexity: "small"
    files_affected:
      - "README.md"

  - id: "TASK-006"
    title: "Verify no dangling references remain"
    description: "Run a repository-wide search for the strings `jms-git-push` and `jp-planner` across all tracked files, excluding the `.plans/` directory. Confirm that zero matches are found for each string. This is a verification task, not an implementation task -- if matches are found, report them so they can be addressed."
    requirements:
      - "REQ-006"
    dependencies:
      - "TASK-005"
    suggested_role: "general"
    acceptance_criteria:
      - "A repository-wide search for jms-git-push returns no matches outside .plans/"
      - "A repository-wide search for jp-planner returns no matches outside .plans/"
    estimated_complexity: "small"
    files_affected:
      - "README.md"

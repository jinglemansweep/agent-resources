# Task list generated from PRD: .plans/2026/03/01/08-fixes-renames/prd.md
# Generated by: /jp-plan-task-breakdown

tasks:
  - id: "TASK-001"
    title: "Move and rename jms-role-* skills to jp-persona-* in jplan plugin"
    description: |
      Move the six domain-role skill directories from plugins/jinglemansweep/skills/ into
      plugins/jplan/skills/, renaming them from jms-role-* to jp-persona-*. The mapping is:

        jms-role-python       -> jp-persona-python
        jms-role-nodejs       -> jp-persona-nodejs
        jms-role-frontend     -> jp-persona-frontend
        jms-role-devops       -> jp-persona-devops
        jms-role-docs         -> jp-persona-docs
        jms-role-agent-skills -> jp-persona-agent-skills

      For each skill directory:
      1. Create the new directory under plugins/jplan/skills/ (e.g. plugins/jplan/skills/jp-persona-python/).
      2. Copy the SKILL.md from the old directory.
      3. In the copied SKILL.md, replace any references to the old skill name (e.g. "jms-role-python")
         with the new name (e.g. "jp-persona-python").
      4. Delete the old directory from plugins/jinglemansweep/skills/.

      Use `git mv` where possible to preserve history. The jms-git skill must remain in
      plugins/jinglemansweep/skills/ unaffected.
    requirements:
      - "REQ-001"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "Each jp-persona-* directory exists under plugins/jplan/skills/ with a SKILL.md file"
      - "Each jms-role-* directory no longer exists under plugins/jinglemansweep/skills/"
      - "SKILL.md files in jp-persona-* directories reference the new name where the old name appeared"
      - "jms-git skill directory remains untouched in plugins/jinglemansweep/skills/"
    estimated_complexity: "medium"
    files_affected:
      - "plugins/jinglemansweep/skills/jms-role-python/SKILL.md"
      - "plugins/jinglemansweep/skills/jms-role-nodejs/SKILL.md"
      - "plugins/jinglemansweep/skills/jms-role-frontend/SKILL.md"
      - "plugins/jinglemansweep/skills/jms-role-devops/SKILL.md"
      - "plugins/jinglemansweep/skills/jms-role-docs/SKILL.md"
      - "plugins/jinglemansweep/skills/jms-role-agent-skills/SKILL.md"
      - "plugins/jplan/skills/jp-persona-python/SKILL.md"
      - "plugins/jplan/skills/jp-persona-nodejs/SKILL.md"
      - "plugins/jplan/skills/jp-persona-frontend/SKILL.md"
      - "plugins/jplan/skills/jp-persona-devops/SKILL.md"
      - "plugins/jplan/skills/jp-persona-docs/SKILL.md"
      - "plugins/jplan/skills/jp-persona-agent-skills/SKILL.md"

  - id: "TASK-002"
    title: "Rename jp-plan-* skill directories to shorter jp-* names"
    description: |
      Rename all twelve jp-plan-* skill directories under plugins/jplan/skills/ to shorter names.
      The mapping is:

        jp-plan-code-review    -> jp-codereview
        jp-plan-execute        -> jp-execute
        jp-plan-fix            -> jp-codereview-fix
        jp-plan-init           -> jp-setup
        jp-plan-new            -> jp-plan
        jp-plan-prd            -> jp-prd
        jp-plan-prd-review     -> jp-prd-review
        jp-plan-summary        -> jp-summary
        jp-plan-task-breakdown -> jp-task-list
        jp-plan-task-review    -> jp-task-review
        jp-plan-task-validate  -> jp-task-validate
        jp-plan-workflow       -> jp-quick

      For each directory, use `git mv` to rename the directory (e.g.
      `git mv plugins/jplan/skills/jp-plan-code-review plugins/jplan/skills/jp-codereview`).
      The SKILL.md content inside each directory will be updated for cross-references in a
      separate task (TASK-004). This task only handles the directory renames.
    requirements:
      - "REQ-003"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "Each old jp-plan-* directory no longer exists under plugins/jplan/skills/"
      - "Each new jp-* directory exists under plugins/jplan/skills/ with its SKILL.md"
      - "All twelve renames are completed per the mapping table"
    estimated_complexity: "medium"
    files_affected:
      - "plugins/jplan/skills/jp-codereview/SKILL.md"
      - "plugins/jplan/skills/jp-execute/SKILL.md"
      - "plugins/jplan/skills/jp-codereview-fix/SKILL.md"
      - "plugins/jplan/skills/jp-setup/SKILL.md"
      - "plugins/jplan/skills/jp-plan/SKILL.md"
      - "plugins/jplan/skills/jp-prd/SKILL.md"
      - "plugins/jplan/skills/jp-prd-review/SKILL.md"
      - "plugins/jplan/skills/jp-summary/SKILL.md"
      - "plugins/jplan/skills/jp-task-list/SKILL.md"
      - "plugins/jplan/skills/jp-task-review/SKILL.md"
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"
      - "plugins/jplan/skills/jp-quick/SKILL.md"

  - id: "TASK-003"
    title: "Rename jp-developer agent to jp-worker-dev"
    description: |
      Rename the agent file from plugins/jplan/agents/jp-developer.md to
      plugins/jplan/agents/jp-worker-dev.md using `git mv`. Then update the file content:

      1. Replace the agent name "jp-developer" with "jp-worker-dev" in the file's title/heading.
      2. Update the domain-skill routing table inside the agent to reference jp-persona-* names
         instead of jms-role-* names:
           jms-role-python       -> jp-persona-python
           jms-role-nodejs       -> jp-persona-nodejs
           jms-role-frontend     -> jp-persona-frontend
           jms-role-devops       -> jp-persona-devops
           jms-role-docs         -> jp-persona-docs
           jms-role-agent-skills -> jp-persona-agent-skills
      3. Update any references to jp-plan-* skill names to their new jp-* equivalents per
         the mapping in TASK-002's description.
    requirements:
      - "REQ-002"
    dependencies: []
    suggested_role: "general"
    acceptance_criteria:
      - "plugins/jplan/agents/jp-developer.md no longer exists"
      - "plugins/jplan/agents/jp-worker-dev.md exists with updated content"
      - "The agent's routing table references jp-persona-* names instead of jms-role-*"
      - "Any jp-plan-* skill references in the agent file use the new jp-* names"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/agents/jp-worker-dev.md"

  - id: "TASK-004"
    title: "Update all cross-references in SKILL.md files"
    description: |
      Scan every SKILL.md file in the repository and update all references to old skill names
      and the old agent name. The replacements to apply are:

      Skill renames (jp-plan-* -> jp-*):
        /jp-plan-code-review    -> /jp-codereview
        /jp-plan-execute        -> /jp-execute
        /jp-plan-fix            -> /jp-codereview-fix
        /jp-plan-init           -> /jp-setup
        /jp-plan-new            -> /jp-plan
        /jp-plan-prd-review     -> /jp-prd-review
        /jp-plan-prd            -> /jp-prd
        /jp-plan-summary        -> /jp-summary
        /jp-plan-task-breakdown -> /jp-task-list
        /jp-plan-task-review    -> /jp-task-review
        /jp-plan-task-validate  -> /jp-task-validate
        /jp-plan-workflow       -> /jp-quick

      Also replace non-slash-prefixed references:
        jp-plan-code-review    -> jp-codereview
        jp-plan-execute        -> jp-execute
        jp-plan-fix            -> jp-codereview-fix
        jp-plan-init           -> jp-setup
        jp-plan-new            -> jp-plan
        jp-plan-prd-review     -> jp-prd-review
        jp-plan-prd            -> jp-prd
        jp-plan-summary        -> jp-summary
        jp-plan-task-breakdown -> jp-task-list
        jp-plan-task-review    -> jp-task-review
        jp-plan-task-validate  -> jp-task-validate
        jp-plan-workflow       -> jp-quick

      Role skill renames (jms-role-* -> jp-persona-*):
        jms-role-python       -> jp-persona-python
        jms-role-nodejs       -> jp-persona-nodejs
        jms-role-frontend     -> jp-persona-frontend
        jms-role-devops       -> jp-persona-devops
        jms-role-docs         -> jp-persona-docs
        jms-role-agent-skills -> jp-persona-agent-skills

      Agent rename:
        jp-developer -> jp-worker-dev

      IMPORTANT: Apply longer patterns first to avoid partial replacements (e.g.
      jp-plan-prd-review before jp-plan-prd, jp-plan-task-breakdown before jp-plan-task-review).

      Files to update: all SKILL.md files under plugins/jplan/skills/ and
      plugins/jinglemansweep/skills/, plus plugins/jplan/agents/jp-worker-dev.md if any
      references remain.
    requirements:
      - "REQ-004"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "general"
    acceptance_criteria:
      - "No SKILL.md file contains any old jp-plan-* skill name"
      - "No SKILL.md file contains any jms-role-* skill name"
      - "No SKILL.md file contains the old agent name jp-developer"
      - "All cross-references use the correct new names per the mapping tables"
    estimated_complexity: "large"
    files_affected:
      - "plugins/jplan/skills/jp-codereview/SKILL.md"
      - "plugins/jplan/skills/jp-execute/SKILL.md"
      - "plugins/jplan/skills/jp-codereview-fix/SKILL.md"
      - "plugins/jplan/skills/jp-setup/SKILL.md"
      - "plugins/jplan/skills/jp-plan/SKILL.md"
      - "plugins/jplan/skills/jp-prd/SKILL.md"
      - "plugins/jplan/skills/jp-prd-review/SKILL.md"
      - "plugins/jplan/skills/jp-summary/SKILL.md"
      - "plugins/jplan/skills/jp-task-list/SKILL.md"
      - "plugins/jplan/skills/jp-task-review/SKILL.md"
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"
      - "plugins/jplan/skills/jp-quick/SKILL.md"
      - "plugins/jplan/skills/jp-persona-python/SKILL.md"
      - "plugins/jplan/skills/jp-persona-nodejs/SKILL.md"
      - "plugins/jplan/skills/jp-persona-frontend/SKILL.md"
      - "plugins/jplan/skills/jp-persona-devops/SKILL.md"
      - "plugins/jplan/skills/jp-persona-docs/SKILL.md"
      - "plugins/jplan/skills/jp-persona-agent-skills/SKILL.md"
      - "plugins/jplan/agents/jp-worker-dev.md"

  - id: "TASK-005"
    title: "Add documentation gate to jp-execute skill"
    description: |
      Edit plugins/jplan/skills/jp-execute/SKILL.md (formerly jp-plan-execute) to add a
      documentation quality gate step. This step must be inserted after all tasks have been
      completed and the review/fix loop has concluded, but before the user is presented with
      push/PR/stop options.

      The new step should:
      1. Invoke the /jp-persona-docs skill to review and update project documentation
         (README.md, inline docs, etc.) ensuring documentation stays current with the changes
         made during the phase.
      2. Be clearly labelled as a documentation review step (e.g. "Step N: Documentation Review").
      3. Include a brief description explaining that this step ensures documentation is updated
         to reflect all changes made during the phase.

      Read the current jp-execute SKILL.md to find the exact insertion point -- it should be
      after the review/fix cycle and before the final push/PR prompt.
    requirements:
      - "REQ-005"
    dependencies:
      - "TASK-002"
      - "TASK-004"
    suggested_role: "skills"
    acceptance_criteria:
      - "jp-execute SKILL.md contains a documentation step that invokes /jp-persona-docs"
      - "The documentation step appears after the review/fix cycle completes"
      - "The documentation step appears before push/PR/stop options are presented to the user"
      - "The step is clearly numbered and documented within the skill's instruction flow"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/jp-execute/SKILL.md"

  - id: "TASK-006"
    title: "Update plugin manifest files (plugin.json)"
    description: |
      Update both plugin manifest files to reflect all renames.

      1. plugins/jplan/plugin.json:
         - Replace the skills array with all new names. The complete list should be:
           "jp-setup", "jp-plan", "jp-prd", "jp-prd-review", "jp-task-list", "jp-task-review",
           "jp-execute", "jp-task-validate", "jp-codereview", "jp-codereview-fix", "jp-summary",
           "jp-quick", "jp-persona-python", "jp-persona-nodejs", "jp-persona-frontend",
           "jp-persona-devops", "jp-persona-docs", "jp-persona-agent-skills"
         - Replace the agents array entry "jp-developer" with "jp-worker-dev".

      2. plugins/jinglemansweep/plugin.json:
         - Remove all six jms-role-* entries from the skills array.
         - The skills array should contain only "jms-git".
         - The agents array should remain empty.

      Both files must remain valid JSON.
    requirements:
      - "REQ-007"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "general"
    acceptance_criteria:
      - "plugins/jplan/plugin.json lists all 18 new skill names (12 jp-* + 6 jp-persona-*)"
      - "plugins/jplan/plugin.json references jp-worker-dev as the agent"
      - "plugins/jinglemansweep/plugin.json only lists jms-git as a skill"
      - "plugins/jinglemansweep/plugin.json has an empty agents array"
      - "Both files are valid JSON"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/plugin.json"
      - "plugins/jinglemansweep/plugin.json"

  - id: "TASK-007"
    title: "Update install.sh scripts for both plugins"
    description: |
      Update the install.sh scripts in both plugins to reflect the renames.

      1. plugins/jplan/install.sh:
         Currently uses a simple `cp -r` to copy all skills and agents. This approach still
         works since the directories themselves are renamed. However, add cleanup commands to
         remove old skill/agent names from the user's ~/.claude/ directory:
         - Remove old jp-plan-* skill directories (all 12 old names)
         - Remove old jp-developer.md agent file
         The cleanup section should follow the same pattern as the existing cleanup in
         plugins/jinglemansweep/install.sh.

      2. plugins/jinglemansweep/install.sh:
         - Add cleanup commands to remove the six jms-role-* skill directories that have been
           moved to the jplan plugin:
             rm -rf "${DEST}/skills/jms-role-python"
             rm -rf "${DEST}/skills/jms-role-nodejs"
             rm -rf "${DEST}/skills/jms-role-frontend"
             rm -rf "${DEST}/skills/jms-role-devops"
             rm -rf "${DEST}/skills/jms-role-docs"
             rm -rf "${DEST}/skills/jms-role-agent-skills"
         - The existing cleanup sections for older renames should remain.

      Both scripts must execute without errors (bash -n syntax check should pass).
    requirements:
      - "REQ-011"
      - "REQ-013"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "devops"
    acceptance_criteria:
      - "plugins/jplan/install.sh includes cleanup of all 12 old jp-plan-* skill directories"
      - "plugins/jplan/install.sh includes cleanup of old jp-developer.md agent file"
      - "plugins/jinglemansweep/install.sh includes cleanup of all 6 jms-role-* skill directories"
      - "Both scripts pass bash -n syntax check"
      - "Running jplan install.sh creates correct skill and agent entries in ~/.claude/"
    estimated_complexity: "medium"
    files_affected:
      - "plugins/jplan/install.sh"
      - "plugins/jinglemansweep/install.sh"

  - id: "TASK-008"
    title: "Update README.md with new names and directory structure"
    description: |
      Update the project README.md at the repository root to reflect all renames:

      1. Update the directory tree to show the new skill directory names (jp-* and jp-persona-*)
         instead of old jp-plan-* and jms-role-* names.
      2. Update the agent reference from jp-developer to jp-worker-dev.
      3. Update any pipeline flow descriptions to use the new skill names.
      4. Update any skill name references in prose, tables, or code blocks.
      5. Ensure no references to old jp-plan-*, jms-role-*, or jp-developer names remain.

      The complete rename mappings are:
        jp-plan-* -> jp-* (12 renames as per TASK-002 mapping)
        jms-role-* -> jp-persona-* (6 renames as per TASK-001 mapping)
        jp-developer -> jp-worker-dev
    requirements:
      - "REQ-008"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "docs"
    acceptance_criteria:
      - "The README directory tree shows all new jp-* and jp-persona-* skill directory names"
      - "The README references jp-worker-dev instead of jp-developer"
      - "No references to old jp-plan-*, jms-role-*, or jp-developer names remain in the README"
      - "The pipeline flow description uses new skill names"
    estimated_complexity: "medium"
    files_affected:
      - "README.md"

  - id: "TASK-009"
    title: "Update .agentmap.yaml with new names"
    description: |
      Update .agentmap.yaml at the repository root to reflect all renames:

      1. In the tree section under plugins/jplan/skills/, replace all jp-plan-* entries with
         their new jp-* names, and add the six jp-persona-* entries.
      2. In the tree section under plugins/jplan/agents/, replace jp-developer.md with
         jp-worker-dev.md.
      3. In the tree section under plugins/jinglemansweep/skills/, remove the six jms-role-*
         entries (only jms-git should remain).
      4. In the conventions section, update any references to old names.
      5. Update the "updated" field in meta to today's date: "2026-03-01".
      6. Ensure the file remains valid YAML.

      Full rename mappings:
        jp-plan-* -> jp-* (12 renames as per TASK-002 mapping)
        jms-role-* -> jp-persona-* (6 renames, now under jplan)
        jp-developer -> jp-worker-dev
    requirements:
      - "REQ-009"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "general"
    acceptance_criteria:
      - "All skill entries use the new jp-* and jp-persona-* names"
      - "The agent entry references jp-worker-dev.md"
      - "No references to old jp-plan-*, jms-role-*, or jp-developer names remain"
      - "The file is valid YAML"
    estimated_complexity: "small"
    files_affected:
      - ".agentmap.yaml"

  - id: "TASK-010"
    title: "Update existing settings.local.json files"
    description: |
      Update the two existing settings.local.json files to use new names and remove stale entries.

      1. .claude/settings.local.json:
         - Remove all stale Skill() entries that reference old names:
           Skill(pq-init), Skill(pq-phase-new), Skill(pq-plan), Skill(pq-review),
           Skill(pq-taskify), Skill(pq-execute), Skill(jms-phase-new), Skill(jms-init),
           Skill(jms-validate), Skill(jms-code-review), Skill(jms-summary),
           Skill(jms-plan-code-review), Skill(jms-plan-summary), Skill(jp-plan-summary)
         - Add Skill() entries for all new skill names:
           jp-setup, jp-plan, jp-prd, jp-prd-review, jp-task-list, jp-task-review,
           jp-execute, jp-task-validate, jp-codereview, jp-codereview-fix, jp-summary, jp-quick,
           jp-persona-python, jp-persona-nodejs, jp-persona-frontend, jp-persona-devops,
           jp-persona-docs, jp-persona-agent-skills, agentmap-read, agentmap-init,
           agentmap-generate, jms-git
         - Keep non-Skill entries (Bash, WebSearch, WebFetch, mcp) as they are.
         - Ensure valid JSON.

      2. plugins/jinglemansweep/.claude/settings.local.json:
         - Remove the Skill(jms-plan-init) entry. Since jms-git is the only remaining skill
           in this plugin, either add Skill(jms-git) or remove the file if no permissions
           are needed.
         - Ensure valid JSON if the file is kept.
    requirements:
      - "REQ-010"
    dependencies:
      - "TASK-001"
      - "TASK-002"
      - "TASK-003"
    suggested_role: "general"
    acceptance_criteria:
      - ".claude/settings.local.json uses new jp-* and jp-persona-* skill names"
      - "All stale pq-*, jms-plan-*, jms-* Skill entries are removed"
      - "plugins/jinglemansweep/.claude/settings.local.json no longer references jms-plan-init"
      - "Both files are valid JSON"
    estimated_complexity: "small"
    files_affected:
      - ".claude/settings.local.json"
      - "plugins/jinglemansweep/.claude/settings.local.json"

  - id: "TASK-011"
    title: "Generate example settings.local.example.json"
    description: |
      Create a new file at the repository root named settings.local.example.json. This file
      serves as a reference that users can copy into their .claude/settings.local.json.

      The file must contain an "allow" permissions array with entries for:

      1. All skills in the repo (using new names):
         - jp-setup, jp-plan, jp-prd, jp-prd-review, jp-task-list, jp-task-review,
           jp-execute, jp-task-validate, jp-codereview, jp-codereview-fix, jp-summary, jp-quick
         - jp-persona-python, jp-persona-nodejs, jp-persona-frontend, jp-persona-devops,
           jp-persona-docs, jp-persona-agent-skills
         - agentmap-read, agentmap-init, agentmap-generate
         - jms-git
         Format: "Skill(skill-name)"

      2. The jp-worker-dev agent:
         Format: "Task(jp-worker-dev)"

      3. Common bash commands used by the skills:
         - "Bash(git:*)"
         - "Bash(gh:*)"
         - "Bash(ls:*)"
         - "Bash(find:*)"
         - "Bash(grep:*)"
         - "Bash(test:*)"
         - "Bash(pre-commit run:*)"

      4. Other tool permissions commonly needed:
         - "WebSearch"
         - "mcp__ide__getDiagnostics"

      The file must be valid JSON following the Claude Code settings schema:
      {
        "permissions": {
          "allow": [ ... ]
        }
      }
    requirements:
      - "REQ-006"
    dependencies:
      - "TASK-006"
    suggested_role: "general"
    acceptance_criteria:
      - "settings.local.example.json exists at the repository root"
      - "Contains Skill() entries for all 22 skills in the repo"
      - "Contains a Task() entry for jp-worker-dev"
      - "Contains Bash() entries for common commands (git, gh, ls, find, grep, test, pre-commit)"
      - "The file is valid JSON following the Claude Code settings schema"
    estimated_complexity: "medium"
    files_affected:
      - "settings.local.example.json"

  - id: "TASK-012"
    title: "Validate no broken cross-references remain"
    description: |
      Run validation grep checks to ensure no old names remain in active files (excluding
      .plans/ directories which contain historical logs).

      1. Grep for all old jp-plan-* skill names outside .plans/:
         jp-plan-code-review, jp-plan-execute, jp-plan-fix, jp-plan-init, jp-plan-new,
         jp-plan-prd, jp-plan-prd-review, jp-plan-summary, jp-plan-task-breakdown,
         jp-plan-task-review, jp-plan-task-validate, jp-plan-workflow

      2. Grep for all old jms-role-* skill names outside .plans/:
         jms-role-python, jms-role-nodejs, jms-role-frontend, jms-role-devops,
         jms-role-docs, jms-role-agent-skills

      3. Grep for the old agent name "jp-developer" outside .plans/ (as a skill/agent
         reference, not a generic word).

      For each grep, exclude the .plans/ directory. If any matches are found, report the
      file and line so they can be fixed. All greps should return zero matches for the
      validation to pass.
    requirements:
      - "REQ-012"
    dependencies:
      - "TASK-004"
      - "TASK-005"
      - "TASK-006"
      - "TASK-007"
      - "TASK-008"
      - "TASK-009"
      - "TASK-010"
      - "TASK-011"
    suggested_role: "general"
    acceptance_criteria:
      - "Grep for all 12 old jp-plan-* names returns no matches outside .plans/"
      - "Grep for all 6 old jms-role-* names returns no matches outside .plans/"
      - "Grep for jp-developer returns no matches outside .plans/"
      - "All validation checks pass with zero hits"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/"
      - "plugins/jinglemansweep/skills/"
      - "plugins/jplan/agents/"
      - "README.md"
      - ".agentmap.yaml"

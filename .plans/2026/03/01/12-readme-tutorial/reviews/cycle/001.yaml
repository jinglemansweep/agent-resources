# Code review round 001
# Phase: .plans/2026/03/01/12-readme-tutorial
# Scope: full
# Generated by: /jp-codereview

issues:
  - id: "ISSUE-001"
    severity: "MAJOR"
    category: "style"
    file: "README.md"
    line: 240
    description: >
      The Stage 7 (/jp-execute) description says "After each task, it runs validation checks
      (`/jp-task-validate`), performs code review (`/jp-codereview`), and applies fixes for critical
      and major issues (`/jp-codereview-fix`)." This conflates per-task validation with the
      post-execution review loop. According to the actual jp-execute skill definition, only
      `/jp-task-validate` runs after each individual task (Step 5e). The code review
      (`/jp-codereview`) and fix application (`/jp-codereview-fix`) run after ALL tasks complete,
      in a separate review-and-fix loop (Step 7). The current wording implies all three tools run
      after every task, which misrepresents the execution flow.
    suggestion: >
      Reword the description to distinguish per-task validation from the post-execution review loop.
      For example: "For each task, it delegates to the `jp-worker-dev` agent and validates the
      output (`/jp-task-validate`). After all tasks complete, it enters a review-and-fix loop:
      performing code review (`/jp-codereview`) and applying fixes for critical and major issues
      (`/jp-codereview-fix`)."
    related_tasks:
      - "TASK-005"

summary:
  total_issues: 1
  critical: 0
  major: 1
  minor: 0
  by_category:
    integration: 0
    architecture: 0
    security: 0
    duplication: 0
    error-handling: 0
    style: 1

# Task list generated from PRD: .plans/2026/03/01/13-validator-improvements/prd.md
# Generated by: /jp-task-list

tasks:
  - id: "TASK-001"
    title: "Add markdownlint and yamllint detection to Step 2"
    description: |
      Update the "Step 2: Detect Project Tooling" section in
      plugins/jplan/skills/jp-task-validate/SKILL.md to add detection entries
      for markdownlint and yamllint. This follows the same pattern as existing
      tool detection entries (ESLint, Ruff, mypy, etc.).

      Add the following detection entries to the bullet list in Step 2:

      - `.markdownlint.yaml`, `.markdownlint.yml`, `.markdownlint.json`,
        `.markdownlint-cli2.yaml`, `.markdownlintrc` -- Markdownlint configuration
      - `.yamllint`, `.yamllint.yaml`, `.yamllint.yml` -- Yamllint configuration

      Also add PATH availability checks: the skill should check whether
      `markdownlint` (or `markdownlint-cli2`) and `yamllint` are available
      as CLI commands, following the same detection approach used for other
      tools like `shellcheck`, `ruff`, and `eslint`.

      The detection entries should be grouped logically with existing entries
      (Markdown linting near other linting tools, YAML near other config formats).
    requirements:
      - "REQ-003"
      - "REQ-006"
    dependencies: []
    suggested_role: "skills"
    acceptance_criteria:
      - "Step 2 includes a bullet for markdownlint config files listing all five filenames: .markdownlint.yaml, .markdownlint.yml, .markdownlint.json, .markdownlint-cli2.yaml, .markdownlintrc"
      - "Step 2 includes a bullet for yamllint config files listing all three filenames: .yamllint, .yamllint.yaml, .yamllint.yml"
      - "Step 2 includes PATH availability detection for markdownlint/markdownlint-cli2 and yamllint CLI commands"
      - "Detection entries follow the same format and style as existing entries in Step 2"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"

  - id: "TASK-002"
    title: "Add markdownlint routing for Markdown files in Step 3"
    description: |
      Add a new "Markdown files" subsection to "Step 3: Syntax Validation and
      Linting" in plugins/jplan/skills/jp-task-validate/SKILL.md. Currently
      there is no linting entry for .md files. The new subsection must follow
      the same structure and pattern used by the existing Python, JavaScript,
      JSON, YAML, and Shell script subsections.

      Add the following subsection (place it after the existing YAML subsection
      and before the Shell scripts subsection, or in a logical position among
      the existing language blocks):

      **Markdown files:**

      1. If `markdownlint` (or `markdownlint-cli2`) is available: run
         `markdownlint <files>` (or `markdownlint-cli2 <files>`) against each
         Markdown file.
      2. If `markdownlint` is not available, skip Markdown-specific linting
         without producing an error.

      The subsection must:
      - Check for tool availability before running (detected in Step 2).
      - Capture markdownlint errors with file path and line number.
      - Include the tool name in error output so the report identifies which
        tool produced each finding (REQ-004).
      - Not produce any error or warning if markdownlint is not installed
        (REQ-005).
      - Follow the same structural pattern as other linter subsections in
        Step 3 (REQ-006).
    requirements:
      - "REQ-001"
      - "REQ-004"
      - "REQ-005"
      - "REQ-006"
    dependencies:
      - "TASK-001"
    suggested_role: "skills"
    acceptance_criteria:
      - "Step 3 contains a 'Markdown files' subsection with markdownlint routing"
      - "Markdownlint is only invoked if detected as available in Step 2"
      - "If markdownlint is not available, no error or warning is produced and Markdown files are silently skipped for linting"
      - "Markdownlint errors are captured with file path and line number where available"
      - "The subsection follows the same format as existing Python, JS, YAML, and Shell subsections in Step 3"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"

  - id: "TASK-003"
    title: "Update yamllint routing for YAML files in Step 3"
    description: |
      Update the existing "YAML files" subsection in "Step 3: Syntax Validation
      and Linting" in plugins/jplan/skills/jp-task-validate/SKILL.md. The
      current YAML subsection (line 88-90) reads:

        **YAML files:**
        1. Syntax check: `python -c "import yaml; yaml.safe_load(open('<file>'))"` for each YAML file, or `yamllint <file>` if `yamllint` is available.

      This needs to be restructured to follow the detection-first pattern used
      by other linters. The updated subsection should:

      1. If `yamllint` is available (detected in Step 2): run `yamllint <files>`
         against each YAML file. Yamllint provides both syntax checking and
         linting, so it replaces the basic Python syntax check.
      2. If `yamllint` is not available: fall back to the existing Python
         `yaml.safe_load` syntax check.

      The restructured subsection must:
      - Prioritize yamllint over the basic syntax check when available.
      - Capture yamllint errors with file path and line number.
      - Include the tool name in error output (REQ-004).
      - Fall back gracefully to basic syntax check if yamllint is not
        installed (REQ-005).
      - Follow the same structural pattern as the Python files subsection
        which also has a tiered tool preference (ruff preferred over flake8)
        (REQ-006).
    requirements:
      - "REQ-002"
      - "REQ-004"
      - "REQ-005"
      - "REQ-006"
    dependencies:
      - "TASK-001"
    suggested_role: "skills"
    acceptance_criteria:
      - "YAML subsection in Step 3 checks for yamllint availability before running"
      - "When yamllint is available, it is used instead of the basic Python yaml.safe_load check"
      - "When yamllint is not available, the existing Python yaml.safe_load syntax check is used as fallback"
      - "Yamllint errors are captured with file path and line number where available"
      - "The subsection follows the tiered preference pattern (yamllint preferred, Python fallback) consistent with the Python subsection's ruff/flake8 pattern"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"

  - id: "TASK-004"
    title: "Verify full SKILL.md consistency and graceful degradation"
    description: |
      Review the complete plugins/jplan/skills/jp-task-validate/SKILL.md file
      after TASK-001, TASK-002, and TASK-003 are applied. Verify that:

      1. The validation report format in Step 8 does not need updates -- the
         existing "Syntax / Linting" section already captures all linter
         findings generically, so markdownlint and yamllint results should
         integrate naturally. Confirm this is the case; if not, update Step 8.
      2. The guidelines section accurately reflects the new tool integrations
         (no updates expected, but verify).
      3. All references to tool detection in Step 2 are consistent with the
         routing in Step 3 -- no tool is routed in Step 3 without being
         detected in Step 2, and no tool is detected in Step 2 without being
         used in Step 3.
      4. Graceful degradation is consistent: every conditional linter invocation
         has a clear fallback or silent skip when the tool is absent.
      5. The file reads coherently as a whole with no formatting inconsistencies,
         dangling references, or style mismatches between the new and existing
         content.

      Make any minor consistency or formatting fixes needed. Do not add new
      features or change behaviour beyond what the PRD requires.
    requirements:
      - "REQ-004"
      - "REQ-005"
      - "REQ-006"
    dependencies:
      - "TASK-002"
      - "TASK-003"
    suggested_role: "skills"
    acceptance_criteria:
      - "Step 2 tool detection entries and Step 3 linter routing entries are fully consistent -- every detected tool is used, every used tool is detected"
      - "Every conditional linter invocation in Step 3 has a clear fallback or silent skip for when the tool is absent"
      - "The validation report format in Step 8 accommodates markdownlint and yamllint findings without requiring structural changes"
      - "No formatting inconsistencies or style mismatches between new and existing content"
    estimated_complexity: "small"
    files_affected:
      - "plugins/jplan/skills/jp-task-validate/SKILL.md"

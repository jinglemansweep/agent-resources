We need a new GitHub Action that runs on Pull Requests to default branch. It will be a mandatory status check. It should install and run pre-commit, the same checks performed locally.

Also improve the "plan" skill around the research area. As well as collecting all external depdencies and summarising them, we should perform analysis to ensure the components are compatible with each other, and perform web and GitHub issue searches to verify all chosen components work well together. Research should also include a licence breakdown, not essential for me, but useful to know at the research stage.

Also, change the README title to "Agentic Dev Resources"
